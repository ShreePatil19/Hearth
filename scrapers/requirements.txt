# Pinned to exact versions for reproducible CI and to avoid a surprise major
# bump (e.g. a Pydantic v3) breaking the daily run. See #97. Bump deliberately.
requests==2.33.0
beautifulsoup4==4.14.3
httpx==0.28.1
python-dotenv==1.2.1
pydantic==2.12.5
pytest==9.0.2
